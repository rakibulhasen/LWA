<?php

// fatchAll (PDO::FETCH_ASSOC)

// fetchAll(PDO::FETCH_NUM)


// GET PLAIN ONE DIMENSIONAL ARRAY
// FIRST COLUMN  FROM SELECT ITEMS FIRST ONE WILL FETCHER
// fetchAll(PDO::FETCH_COLUMN)


//This mode groups the returned row into a sented array, whre indexes wil be unique values from the first column, and values will be arrays similar to ones returned by regular fetchAll()
// fetchAll(PDO::FETCH_GROUP)


// This one groups the first column with all values from the second column.
// fetchAll(PDO::FETCH_GROUP | PDO::FETCH_COLUMN)



// Retrieves data from a database query as an associative array and a numeric array
// fetchAll(PDO::FETCH_BOTH)

// Distring get dupice get one


// MIN 

// Count

// Sum

// AVG